remotejs is a fork of rotbotjs. It enable a full remote control over a raspberry, using nodejs client/server application.

Steps:

In server side :
node remotejs/tcpIp/server.js

In client side:
node remotejs/tcpIp/client.js


Work has just started!
